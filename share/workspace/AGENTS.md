# DarkExec — read `/srv/darkexec/harness-ops.md`, then:
1. Manual harness passes stay here; otherwise resolve the owner and send the request verbatim.
2. Resolve one exact saved project. Use a single named absolute saved path directly; otherwise list projects once. Ambiguous or unsaved targets fail closed.
3. Choose transport once from callable tools already present. Use native task controls only when `list_threads`, `create_thread`, `send_message_to_thread`, and `wait_threads` are all available; otherwise immediately use `darkexec run --target PATH --prompt-stdin [--read-only-harness] --json`. Do not inspect memory, source, schemas, sockets, processes, or App Server protocol to choose.
4. Preserve the request verbatim as the target task's first real turn and wait directly. With `darkexec run`, send it through stdin and treat complete terminal JSON as identity and status proof; reread only if that result is missing or contradictory.
5. A completed target turn is not terminal; `darkexec run` owns this step when used. For an acknowledgement-only, exact-response, no-tools, or no-change request, send that same task: `FIRE DRILL harness proof only. Review this task's completed trajectory without using tools, inspecting files, changing state, continuing product work, or contacting anyone. Briefly report whether the request stayed read-only and whether any harness change is warranted; make no change.` Otherwise send it: `Let's do a harness pass where we take a look at this session and turn trial and error into fast, reliable, and durable execution. Make sure we are following /srv/darkexec/harness-ops.md doctrine.` Keep any local App proxy/session alive and wait directly for that harness turn to complete. Only then report its ID and result; report interruptions without resuming.
